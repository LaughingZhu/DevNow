---
import { getFormattedDate } from '@utils/all';

export interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time class='text-sm font-bold text-opacity-60' datetime={getFormattedDate(date)} }>
  {getFormattedDate(date)}
</time>
