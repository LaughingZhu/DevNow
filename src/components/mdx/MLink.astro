---
import { LinkPreview } from '../LinkPreview';

const props = Astro.props;
const { href, rel = 'noopener noreferrer' } = props;
---

<LinkPreview client:only='react' url={href}>
  <slot />
</LinkPreview>
